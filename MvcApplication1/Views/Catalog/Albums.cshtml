@using Nwd.BackOffice.Model;
@model List<Album>

@{
    ViewBag.Title = "Albums";
}

<h2>Albums</h2>

<ul>
    @foreach (var album in Model)
    {
        <li>@Html.ActionLink(album.Title, "Album", "Catalog", new {id = album.Id}, null)</li>
    }
</ul>